pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "**/*.{ts,tsx,js,jsx}"
      run: pnpm exec oxlint --fix {staged_files}
    oxformat:
      glob: "**/*.{ts,tsx,js,jsx}"
      run: pnpm exec oxfmt --check {staged_files}

pre-push:
  commands:
    test:
      run: pnpm test
