pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "**/*.{ts,tsx,js,jsx}"
      run: oxlint --fix {staged_files}
    oxformat:
      glob: "**/*.{ts,tsx,js,jsx}"
      run: oxfmt --check {staged_files}

pre-push:
  commands:
    test:
      run: pnpm test
